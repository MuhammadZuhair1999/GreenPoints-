import React, {Component} from 'react';

export default class GreenPointsList extends Component{
    render(){ 
        return(
        <div>
           <p> You are on the Green Point List Component!</p>
        </div>
        )}
    
}